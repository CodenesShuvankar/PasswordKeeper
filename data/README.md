# Data Directory

This directory stores the encrypted password database and user data.

- `credentials.db` - Encrypted SQLite database with password entries
- Backup files (if enabled)
- User configuration files

**Important**: Keep this directory secure and backed up regularly!